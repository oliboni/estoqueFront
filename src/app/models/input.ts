import {Product} from './product';
import {Provider} from './provider';

export class Input {
  idInput: number;
  date: Date;
  amount: number;
  unitPrice: number;
  totalPrice: number;
  Product: Product;
  Provider: Provider;
}
